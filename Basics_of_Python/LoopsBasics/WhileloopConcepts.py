#print something to understand the while loop concepts
#nested while
x = 1
                                       #initiation
while x<=5:                                 #condition
    print(x)
    y = 1
    #Y=1 is declared in the loop because if you write outside the loop the
    # values will always be 2 after the loop ended to make it 1 you should include it inside the loop
    while y<=2:
        print("tarak")
        y=y+1
    x=x+1                                   # increment/ decrement